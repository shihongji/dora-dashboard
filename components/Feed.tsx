'use client'; // Add this to ensure it's treated as a client component
const Feed = () => {
    return (
      <div className="bg-gray-400 p-4 mt-4">
        <h2>Feed</h2>
        <p>This is the Feed content below Pad and Player.</p>
      </div>
    );
  };
  
  export default Feed;